# API_KEY
🔐 API KEY


 A very simple thing that used very much nowdays in AI Tools that is API KEY. so i just build a normal python code , index.html and two file where we store the api keys. This is how ai companies doesn't matter big or small make api key like this.


 You can test it 
